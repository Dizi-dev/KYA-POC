[pytest]
testpaths = tests
markers =
    interop: cross-checks against Cloudflare's web-bot-auth JS library (needs node + npm install in interop/js)
    network: talks to live public endpoints (run with KYA_NETWORK=1)
